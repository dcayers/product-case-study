{
  "private": true,
  "scripts": {
    "dev": "prisma generate && next dev",
    "build": "prisma generate && prisma db push && prisma db seed && next build",
    "start": "next start",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "reset": "prisma migrate reset",
    "seed": "prisma db seed",
    "vercel-build": "prisma generate --no-engine && prisma db push && prisma db seed && next build"
  },
  "dependencies": {
    "@pothos/core": "^3.38.0",
    "@pothos/plugin-prisma": "^3.61.0",
    "@prisma/client": "^5.4.2",
    "@prisma/extension-accelerate": "^0.6.2",
    "graphql": "^16.8.1",
    "graphql-scalars": "^1.22.4",
    "graphql-yoga": "^4.0.5",
    "next": "^13.5.4",
    "prisma": "^5.4.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "20.8.4",
    "@types/react": "18.2.28",
    "typescript": "5.2.2"
  },
  "prisma": {
    "seed": "npx ts-node --transpile-only prisma/seed.ts"
  }
}
